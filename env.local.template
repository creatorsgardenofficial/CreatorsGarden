# Stripe API Keys
# Stripeダッシュボード（https://dashboard.stripe.com）から取得してください
# 「開発者」→「APIキー」から以下を取得：
# - シークレットキー（sk_test_...で始まる）
# - 公開可能キー（pk_test_...で始まる）
STRIPE_SECRET_KEY=sk_test_YOUR_SECRET_KEY_HERE
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_YOUR_PUBLISHABLE_KEY_HERE

# Stripe Webhook Secret
# Stripeダッシュボードで「開発者」→「Webhook」から取得
# 開発環境ではStripe CLIを使用する場合、`stripe listen`コマンドで取得できます
STRIPE_WEBHOOK_SECRET=whsec_YOUR_WEBHOOK_SECRET_HERE

# Stripe Price IDs
# Stripeダッシュボードで「製品」→「価格を追加」から作成してください
# 価格タイプ: 定期（サブスクリプション）
# 価格: ¥200
# 通貨: JPY（日本円）
# 請求期間: 月次
# 価格ID（price_...で始まる）をコピーして以下に設定
STRIPE_PRICE_ID_GROW=price_YOUR_GROW_PLAN_PRICE_ID_HERE
STRIPE_PRICE_ID_BLOOM=price_YOUR_BLOOM_PLAN_PRICE_ID_HERE

# ベースURL
# 開発環境
NEXT_PUBLIC_BASE_URL=http://localhost:3000
# 本番環境の場合は以下に変更
# NEXT_PUBLIC_BASE_URL=https://yourdomain.com

# メール送信設定（SMTP）
# メール送信を使用する場合は、以下を設定してください
# Gmailを使用する場合の例:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM_EMAIL=your-email@gmail.com
# SMTP_FROM_NAME=Creators Garden

# その他のメールサービス:
# - Outlook: smtp-mail.outlook.com (ポート587)
# - Yahoo: smtp.mail.yahoo.com (ポート587)
# - カスタムSMTP: サーバー情報を設定

# SMTP設定がない場合、開発環境ではメール内容がdata/emails/フォルダに保存されます

# A8.net広告コード
# A8.netの広告コードを設定してください
# 
# 【単一の広告コードの場合】
# 例: <script type="text/javascript" src="https://statics.a8.net/a8sales/a8sales.js"></script>
#     <a href="https://px.a8.net/svt/ejp?a8mat=..." target="_blank" rel="nofollow">...</a>
#
# 【複数の広告コードをランダム表示する場合】
# JSON配列形式で設定してください。表示するたびにランダムに1つが選択されます。
# 例: ["<script>...</script><a href=\"...\">...</a>", "<script>...</script><a href=\"...\">...</a>"]
# 
# 注意: JSON配列形式の場合、各広告コード内のダブルクォートはエスケープ（\"）してください
NEXT_PUBLIC_A8_AD_CODE=

