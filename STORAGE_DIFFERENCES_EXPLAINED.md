# ファイルシステムとデータベースの挙動の違いについて

## 現在の状況

- **Vercel（本番環境）**: PostgreSQLデータベースを使用
- **ローカル環境**: ファイルシステム（JSONファイル）を使用

この違いにより、以下のような挙動の違いが発生する可能性があります。

## 主な違い

### 1. DMメッセージの既読フラグ

**ファイルシステム版:**
- JSONファイルに`read: boolean`として保存
- 非同期書き込みのため、書き込み完了前に読み取りが発生する可能性がある

**データベース版:**
- `is_read`カラム（boolean）として保存
- トランザクション内で実行されるため、一貫性が保たれる

**影響:**
- ファイルシステム版では、既読フラグの更新が反映される前に読み取りが発生する可能性がある
- データベース版では、トランザクションにより一貫性が保たれる

### 2. GroupMessageのreadBy

**ファイルシステム版:**
- `readBy: string[]`として必ず配列で保存
- 常に配列として扱われる

**データベース版:**
- `read_by`カラム（TEXT[]）として保存
- カラムが存在しない場合、`undefined`になる可能性がある
- nullの場合は空配列に変換される

**影響:**
- データベース版では、スキーマが古い場合に`readBy`が`undefined`になる可能性がある
- ファイルシステム版では常に配列として扱われる

### 3. ソート順

**ファイルシステム版:**
```typescript
.sort((a, b) => new Date(a.createdAt).getTime() - new Date(b.createdAt).getTime())
```

**データベース版:**
```sql
ORDER BY created_at ASC
```

**影響:**
- 基本的に同じだが、タイミングによって順序が変わる可能性がある
- ファイルシステム版はメモリ内でソート、データベース版はDB側でソート

### 4. 既読処理のタイミング

**ファイルシステム版:**
- `fs.writeFile()`は非同期
- 書き込み完了前に読み取りが発生する可能性
- レースコンディションが発生する可能性

**データベース版:**
- SQLの`UPDATE`はトランザクション内で実行
- 一貫性が保たれやすい

**影響:**
- ファイルシステム版では、複数のリクエストが同時に発生した場合、データの不整合が発生する可能性がある
- データベース版では、トランザクションにより一貫性が保たれる

### 5. 同時アクセス

**ファイルシステム版:**
- 複数のプロセスが同じファイルに同時に書き込むと、データが失われる可能性がある
- ファイルロックの仕組みがない

**データベース版:**
- トランザクションとロックにより、同時アクセスでも一貫性が保たれる

**影響:**
- ファイルシステム版では、複数のユーザーが同時に操作すると、データが失われる可能性がある
- データベース版では、トランザクションにより一貫性が保たれる

## 解決策

### 推奨: ローカルでもPostgreSQLを使用

ローカル環境でもPostgreSQLを使用することで、以下のメリットがあります：

1. **本番環境と同じ挙動**
   - 開発環境と本番環境で同じ実装を使用
   - 本番環境で発生する問題をローカルで再現できる

2. **一貫性の向上**
   - トランザクションにより、データの一貫性が保たれる
   - レースコンディションが発生しない

3. **実装の違いを回避**
   - ファイルシステム版とデータベース版の実装の違いを完全に回避

## 設定方法

### ステップ1: `.env.local`の設定

`.env.local`ファイルに以下を追加：

```env
# データベースを使用する設定
USE_DATABASE=true

# ローカルPostgreSQL接続文字列
POSTGRES_URL=postgres://postgres:password@localhost:5432/creators_garden_dev
```

### ステップ2: データベースの作成

```bash
# PostgreSQLに接続
psql -U postgres

# データベースを作成
CREATE DATABASE creators_garden_dev;

# 終了
\q
```

### ステップ3: スキーマの作成

```bash
npm run db:schema
```

### ステップ4: 開発サーバーの起動

```bash
npm run dev
```

## 確認方法

開発サーバーを起動した後、以下のログが表示されることを確認：

```
✅ Using connection string from: POSTGRES_URL
✅ PostgreSQL Pool created successfully
```

また、`http://localhost:3000/api/db/status`にアクセスして、データベース接続の状態を確認できます。

## まとめ

**機能の挙動が異なるのは、DBが異なるからです。**

- ファイルシステム版: 非同期書き込み、レースコンディションの可能性
- データベース版: トランザクション、一貫性が保たれる

ローカルでもPostgreSQLを使用することで、本番環境と同じ挙動で開発・テストができます。


